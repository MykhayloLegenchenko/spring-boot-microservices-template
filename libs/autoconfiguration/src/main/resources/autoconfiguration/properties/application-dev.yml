spring:
  cloud:
    config:
      enabled: false

auth:
  jwt:
    access-token:
      secret: your-access-secret

services:
  discovery: http://localhost:8761

logging:
  level:
    io.opentelemetry.exporter.internal: OFF
