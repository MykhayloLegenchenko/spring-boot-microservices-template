spring:
  kafka:
    consumer:
      properties:
        spring.json.trusted.packages: "com.example.client.users.role.dto, com.example.client.users.user.dto"

  security:
    oauth2:
      client:
        registration:
          google:
            clientId: test-google-client-id
            clientSecret: test-google-client-secret

auth:
  jwt:
    refresh-token:
      ttl: 1800
      secret: test-refresh-secret
